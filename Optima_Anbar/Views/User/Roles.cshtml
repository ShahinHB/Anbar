@model List<IdentityRole>

@{
    ViewData["Title"] = "Roles";
}

<div class="container">
    <div class="row">
        <div class="col-lg-12">
            <a class="btn btn-primary mb-3" asp-controller="User" asp-action="RoleCreate">Vəzifə əlavə et</a>
        </div>
        <hr />
        <div class="col-lg-12 mt-3">
            <table class="table">
                <thead>
                    <tr>
                        <th scope="col">#</th>
                        <th scope="col">Adı</th>
                        <th scope="col"></th>
                    </tr>
                </thead>
                <tbody>
                    @{ int i = 0;
                        foreach (var role in Model)
                        {
                            i++;
                            <tr>
                                <th scope="row">@i</th>
                                <td>@role.Name</td>
                                <td>
                                </td>
                            </tr>
                        }
                    }
                </tbody>
            </table>

        </div>
    </div>
</div>